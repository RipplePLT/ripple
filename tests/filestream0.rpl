void printline(int str){
	print(str);
}

int str_to_int(string str2){
	return 0;
}

void main(){
	int s;
	link(s <- str_to_int <- file_stream("hello.txt", 5, ",")) then printline;
	while(true) {  }
}
